/** The states a Lendo input field can be, each one has specific color:
 * * default: grey.
 * * focus: blue.
 * * error: red.
 * * success: green.
 */
export type InputState = "default" | "focus" | "error" | "success";
